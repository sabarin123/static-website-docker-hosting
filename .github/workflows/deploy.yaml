  - name: Debug SSH connection & tools on EC2
    uses: appleboy/ssh-action@v0.1.8
      with:
          host: ${{ secrets.EC2_SSH_HOST }}
          username: ${{ secrets.EC2_SSH_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            echo "== whoami =="
            whoami || true
            echo "== date =="
            date || true
            echo "== Check docker =="
            docker --version || echo "docker not installed"
            echo "== Check aws cli =="
            aws --version || echo "aws cli not installed"
            echo "== Check docker daemon status =="
            sudo systemctl is-active docker || sudo service docker status || echo "docker service not active"
            echo "== List /logs directory =="
            ls -la /logs || echo "/logs missing"
            echo "== Check ECR login (dry) =="
            aws ecr get-login-password --region "${{ secrets.AWS_REGION }}" >/dev/null 2>&1 && echo "ECR login command works" || echo "ECR login failed"
# Generated by GitHub Copilot Chat Assistant
name: CI/CD Spring Boot → ECR → EC2 → CloudWatch

on:
  push:
    branches:
      - main

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
  ECR_REPO: ${{ secrets.ECR_REPO }}
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build Spring Boot JAR
        run: mvn -B clean package -DskipTests

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build Docker image
        run: |
          docker build -t "${ECR_REPO}:${IMAGE_TAG}" .
          docker tag "${ECR_REPO}:${IMAGE_TAG}" "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO}:${IMAGE_TAG}"

      - name: Push Docker image to ECR
        run: |
          aws ecr describe-repositories --repository-names "${ECR_REPO}" --region "${AWS_REGION}" || \
          aws ecr create-repository --repository-name "${ECR_REPO}" --region "${AWS_REGION}"
          docker push "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO}:${IMAGE_TAG}"

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.EC2_SSH_HOST }}
          username: ${{ secrets.EC2_SSH_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            # On the remote EC2 instance, ensure it has permission to access ECR (IAM role or credentials).
            # Login to ECR on the EC2 instance (requires aws cli and appropriate credentials on the EC2 host).
            aws ecr get-login-password --region "${AWS_REGION}" | docker login --username AWS --password-stdin "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
            docker pull "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO}:${IMAGE_TAG}"
            docker stop simple-website-app || true
            docker rm simple-website-app || true
            docker run -d --name simple-website-app -p 8080:8080 -v /logs:/logs --restart unless-stopped "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPO}:${IMAGE_TAG}"

      - name: Check CloudWatch logs for errors
        run: |
          echo "Fetching last 10 ERROR log entries from CloudWatch..."
          aws logs filter-log-events \
            --log-group-name simpleWebsiteApp-logs \
            --filter-pattern "ERROR" \
            --limit 10
        # aws-actions/configure-aws-credentials already configured credentials for this runner,
        # so explicit env pass is not required here. Keep secrets present in repo settings.
